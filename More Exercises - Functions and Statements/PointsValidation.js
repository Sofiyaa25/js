function isTheDistanceValid(input){
    let str = input.toString();
    let arr = str.split('').map(function(item) {
        return parseInt(item, 10);
    });

    let x1 = arr[0];
    let y1= arr[1];
    let x2 = arr[2];
    let y2= arr[3];
}

function checkTheDistance(x, y){
    if(x==y){
        
    }
}